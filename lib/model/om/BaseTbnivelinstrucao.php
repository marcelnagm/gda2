<?php

/**
 * Base class that represents a row from the 'tbnivelinstrucao' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.0 on:
 *
 * Mon Mar 16 17:38:45 2015
 *
 * @package    lib.model.om
 */
abstract class BaseTbnivelinstrucao extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        TbnivelinstrucaoPeer
	 */
	protected static $peer;

	/**
	 * The value for the id_nivel_instrucao field.
	 * @var        int
	 */
	protected $id_nivel_instrucao;

	/**
	 * The value for the descricao field.
	 * @var        string
	 */
	protected $descricao;

	/**
	 * @var        array Tbalunobackup[] Collection to store aggregation of Tbalunobackup objects.
	 */
	protected $collTbalunobackupsRelatedByPaiIdNivelInstrucao;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTbalunobackupsRelatedByPaiIdNivelInstrucao.
	 */
	private $lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = null;

	/**
	 * @var        array Tbalunobackup[] Collection to store aggregation of Tbalunobackup objects.
	 */
	protected $collTbalunobackupsRelatedByMaeIdNivelInstrucao;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTbalunobackupsRelatedByMaeIdNivelInstrucao.
	 */
	private $lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'TbnivelinstrucaoPeer';

	/**
	 * Get the [id_nivel_instrucao] column value.
	 * 
	 * @return     int
	 */
	public function getIdNivelInstrucao()
	{
		return $this->id_nivel_instrucao;
	}

	/**
	 * Get the [descricao] column value.
	 * 
	 * @return     string
	 */
	public function getDescricao()
	{
		return $this->descricao;
	}

	/**
	 * Set the value of [id_nivel_instrucao] column.
	 * 
	 * @param      int $v new value
	 * @return     Tbnivelinstrucao The current object (for fluent API support)
	 */
	public function setIdNivelInstrucao($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_nivel_instrucao !== $v) {
			$this->id_nivel_instrucao = $v;
			$this->modifiedColumns[] = TbnivelinstrucaoPeer::ID_NIVEL_INSTRUCAO;
		}

		return $this;
	} // setIdNivelInstrucao()

	/**
	 * Set the value of [descricao] column.
	 * 
	 * @param      string $v new value
	 * @return     Tbnivelinstrucao The current object (for fluent API support)
	 */
	public function setDescricao($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->descricao !== $v) {
			$this->descricao = $v;
			$this->modifiedColumns[] = TbnivelinstrucaoPeer::DESCRICAO;
		}

		return $this;
	} // setDescricao()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id_nivel_instrucao = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->descricao = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 2; // 2 = TbnivelinstrucaoPeer::NUM_COLUMNS - TbnivelinstrucaoPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Tbnivelinstrucao object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TbnivelinstrucaoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = TbnivelinstrucaoPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = null;
			$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = null;

			$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = null;
			$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TbnivelinstrucaoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseTbnivelinstrucao:delete:pre') as $callable)
			{
			  if ($ret = call_user_func($callable, $this, $con))
			  {
			    return;
			  }
			}

			if ($ret) {
				TbnivelinstrucaoPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseTbnivelinstrucao:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TbnivelinstrucaoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseTbnivelinstrucao:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseTbnivelinstrucao:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				$con->commit();
				TbnivelinstrucaoPeer::addInstanceToPool($this);
				return $affectedRows;
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = TbnivelinstrucaoPeer::ID_NIVEL_INSTRUCAO;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = TbnivelinstrucaoPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setIdNivelInstrucao($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += TbnivelinstrucaoPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao !== null) {
				foreach ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao !== null) {
				foreach ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = TbnivelinstrucaoPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao !== null) {
					foreach ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao !== null) {
					foreach ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TbnivelinstrucaoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getIdNivelInstrucao();
				break;
			case 1:
				return $this->getDescricao();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = TbnivelinstrucaoPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getIdNivelInstrucao(),
			$keys[1] => $this->getDescricao(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TbnivelinstrucaoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setIdNivelInstrucao($value);
				break;
			case 1:
				$this->setDescricao($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = TbnivelinstrucaoPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setIdNivelInstrucao($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDescricao($arr[$keys[1]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);

		if ($this->isColumnModified(TbnivelinstrucaoPeer::ID_NIVEL_INSTRUCAO)) $criteria->add(TbnivelinstrucaoPeer::ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);
		if ($this->isColumnModified(TbnivelinstrucaoPeer::DESCRICAO)) $criteria->add(TbnivelinstrucaoPeer::DESCRICAO, $this->descricao);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);

		$criteria->add(TbnivelinstrucaoPeer::ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getIdNivelInstrucao();
	}

	/**
	 * Generic method to set the primary key (id_nivel_instrucao column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setIdNivelInstrucao($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Tbnivelinstrucao (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setDescricao($this->descricao);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getTbalunobackupsRelatedByPaiIdNivelInstrucao() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTbalunobackupRelatedByPaiIdNivelInstrucao($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTbalunobackupsRelatedByMaeIdNivelInstrucao() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTbalunobackupRelatedByMaeIdNivelInstrucao($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setIdNivelInstrucao(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Tbnivelinstrucao Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     TbnivelinstrucaoPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new TbnivelinstrucaoPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collTbalunobackupsRelatedByPaiIdNivelInstrucao collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTbalunobackupsRelatedByPaiIdNivelInstrucao()
	 */
	public function clearTbalunobackupsRelatedByPaiIdNivelInstrucao()
	{
		$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTbalunobackupsRelatedByPaiIdNivelInstrucao collection (array).
	 *
	 * By default this just sets the collTbalunobackupsRelatedByPaiIdNivelInstrucao collection to an empty array (like clearcollTbalunobackupsRelatedByPaiIdNivelInstrucao());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTbalunobackupsRelatedByPaiIdNivelInstrucao()
	{
		$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
	}

	/**
	 * Gets an array of Tbalunobackup objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao has previously been saved, it will retrieve
	 * related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage. If this Tbnivelinstrucao is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Tbalunobackup[]
	 * @throws     PropelException
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucao($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
			   $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				TbalunobackupPeer::addSelectColumns($criteria);
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				TbalunobackupPeer::addSelectColumns($criteria);
				if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
					$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;
		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}

	/**
	 * Returns the number of related Tbalunobackup objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Tbalunobackup objects.
	 * @throws     PropelException
	 */
	public function countTbalunobackupsRelatedByPaiIdNivelInstrucao(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$count = TbalunobackupPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
					$count = TbalunobackupPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao);
				}
			} else {
				$count = count($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Tbalunobackup object to this object
	 * through the Tbalunobackup foreign key attribute.
	 *
	 * @param      Tbalunobackup $l Tbalunobackup
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTbalunobackupRelatedByPaiIdNivelInstrucao(Tbalunobackup $l)
	{
		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			$this->initTbalunobackupsRelatedByPaiIdNivelInstrucao();
		}
		if (!in_array($l, $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao, $l);
			$l->setTbnivelinstrucaoRelatedByPaiIdNivelInstrucao($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbnecesespecial($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbnecesespecial($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbnecesespecial($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbcidade($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbcidade($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbcidade($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbpais($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbpais($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbpais($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTblogradouroRelatedByCep($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTblogradouroRelatedByCep($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTblogradouroRelatedByCep($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbcursoversao($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbcursoversao($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbcursoversao($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbtipoingresso($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbtipoingresso($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbtipoingresso($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbalunosituacao($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbalunosituacao($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbalunosituacao($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbinstexternaRelatedByIdDestino($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedByIdDestino($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedByIdDestino($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbinstexternaRelatedById2grau($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedById2grau($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedById2grau($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbinstexternaRelatedById3grau($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedById3grau($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedById3grau($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbinstexternaRelatedByIdTrabalho($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedByIdTrabalho($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedByIdTrabalho($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTblogradouroRelatedByCepTrabalho($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTblogradouroRelatedByCepTrabalho($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTblogradouroRelatedByCepTrabalho($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbpolos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbpolos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbpolos($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbalunoracacor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbalunoracacor($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbalunoracacor($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByPaiIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByPaiIdNivelInstrucaoJoinTbreligiao($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbreligiao($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::PAI_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbreligiao($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByPaiIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao;
	}

	/**
	 * Clears out the collTbalunobackupsRelatedByMaeIdNivelInstrucao collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTbalunobackupsRelatedByMaeIdNivelInstrucao()
	 */
	public function clearTbalunobackupsRelatedByMaeIdNivelInstrucao()
	{
		$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTbalunobackupsRelatedByMaeIdNivelInstrucao collection (array).
	 *
	 * By default this just sets the collTbalunobackupsRelatedByMaeIdNivelInstrucao collection to an empty array (like clearcollTbalunobackupsRelatedByMaeIdNivelInstrucao());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTbalunobackupsRelatedByMaeIdNivelInstrucao()
	{
		$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
	}

	/**
	 * Gets an array of Tbalunobackup objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao has previously been saved, it will retrieve
	 * related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage. If this Tbnivelinstrucao is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Tbalunobackup[]
	 * @throws     PropelException
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucao($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
			   $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				TbalunobackupPeer::addSelectColumns($criteria);
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				TbalunobackupPeer::addSelectColumns($criteria);
				if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
					$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;
		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}

	/**
	 * Returns the number of related Tbalunobackup objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Tbalunobackup objects.
	 * @throws     PropelException
	 */
	public function countTbalunobackupsRelatedByMaeIdNivelInstrucao(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$count = TbalunobackupPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
					$count = TbalunobackupPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao);
				}
			} else {
				$count = count($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Tbalunobackup object to this object
	 * through the Tbalunobackup foreign key attribute.
	 *
	 * @param      Tbalunobackup $l Tbalunobackup
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTbalunobackupRelatedByMaeIdNivelInstrucao(Tbalunobackup $l)
	{
		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			$this->initTbalunobackupsRelatedByMaeIdNivelInstrucao();
		}
		if (!in_array($l, $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao, $l);
			$l->setTbnivelinstrucaoRelatedByMaeIdNivelInstrucao($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbnecesespecial($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbnecesespecial($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbnecesespecial($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbcidade($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbcidade($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbcidade($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbpais($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbpais($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbpais($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTblogradouroRelatedByCep($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTblogradouroRelatedByCep($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTblogradouroRelatedByCep($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbcursoversao($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbcursoversao($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbcursoversao($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbtipoingresso($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbtipoingresso($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbtipoingresso($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbalunosituacao($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbalunosituacao($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbalunosituacao($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbinstexternaRelatedByIdDestino($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedByIdDestino($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedByIdDestino($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbinstexternaRelatedById2grau($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedById2grau($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedById2grau($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbinstexternaRelatedById3grau($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedById3grau($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedById3grau($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbinstexternaRelatedByIdTrabalho($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedByIdTrabalho($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbinstexternaRelatedByIdTrabalho($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTblogradouroRelatedByCepTrabalho($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTblogradouroRelatedByCepTrabalho($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTblogradouroRelatedByCepTrabalho($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbpolos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbpolos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbpolos($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbalunoracacor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbalunoracacor($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbalunoracacor($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tbnivelinstrucao is new, it will return
	 * an empty collection; or if this Tbnivelinstrucao has previously
	 * been saved, it will retrieve related TbalunobackupsRelatedByMaeIdNivelInstrucao from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tbnivelinstrucao.
	 */
	public function getTbalunobackupsRelatedByMaeIdNivelInstrucaoJoinTbreligiao($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TbnivelinstrucaoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao === null) {
			if ($this->isNew()) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = array();
			} else {

				$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbreligiao($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TbalunobackupPeer::MAE_ID_NIVEL_INSTRUCAO, $this->id_nivel_instrucao);

			if (!isset($this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria) || !$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria->equals($criteria)) {
				$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = TbalunobackupPeer::doSelectJoinTbreligiao($criteria, $con, $join_behavior);
			}
		}
		$this->lastTbalunobackupRelatedByMaeIdNivelInstrucaoCriteria = $criteria;

		return $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collTbalunobackupsRelatedByPaiIdNivelInstrucao) {
				foreach ((array) $this->collTbalunobackupsRelatedByPaiIdNivelInstrucao as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTbalunobackupsRelatedByMaeIdNivelInstrucao) {
				foreach ((array) $this->collTbalunobackupsRelatedByMaeIdNivelInstrucao as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collTbalunobackupsRelatedByPaiIdNivelInstrucao = null;
		$this->collTbalunobackupsRelatedByMaeIdNivelInstrucao = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseTbnivelinstrucao:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseTbnivelinstrucao::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseTbnivelinstrucao
