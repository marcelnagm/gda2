<?php

/**
 * Skeleton subclass for performing query and update operations on the 'tbloadaluno' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.0 on:
 *
 * Mon Feb 21 16:11:04 2011
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    lib.model
 */
class TbloadalunoPeer extends BaseTbloadalunoPeer {

    public static function validateData(Tbloadaluno $data, $row) {
        $errorstr = "Erro na linha " . $row . " - ";
        $val = new Validate();

        if (!$val->number($data->getmatricula(), false)) {
            $errorstr .= "Matricula inválida: " . $data->getmatricula() . "; ";
        }
        if (!$data->getemail() == "") {
            if (!$val->email($data->getemail())) {
                $errorstr .= "Email inválido: " . $data->getemail() . "; ";
            }
        }
//        if (!$val->string($data->getdisciplina())) {
//            $errorstr .= "Matricula inválida: " . $data->getmatricula() . "; ";
//        }
//        if (!$val->string($data->getnome(), array())) {
//            $errorstr .= "Nome inválido: " . $data->getnome() . ";";
//        }

        if ($errorstr == "Erro na linha " . $row . " - ") {
            return "ok";
        } else {
            return $errorstr;
        }
    }

}

// TbloadalunoPeer
